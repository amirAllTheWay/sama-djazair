# Rédaction de l'article — optionnel
# Clé gratuite sur https://aistudio.google.com/apikey (aucune carte bancaire).
# Sans cette clé, un brouillon structuré est produit sans IA.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# Affiliation — optionnel, les deux plateformes sont interrogées et la
# première qui répond fournit le lien.

# ShopMy : clé depuis ton compte créateur
SHOPMY_API_KEY=

# impact.com : identifiants publisher + l'id du programme sur lequel tu es validé
IMPACT_ACCOUNT_SID=
IMPACT_AUTH_TOKEN=
IMPACT_PROGRAM_ID=

# Résolution des liens Google News — optionnel mais recommandé
# Les liens Google News redirigent en JavaScript : seul un vrai navigateur les
# suit. Playwright est installé en dépendance optionnelle ; il reste à
# télécharger Chromium une fois :
#     npx playwright install chromium
# Si tu as déjà Chrome ou Chromium, pointe-le ici pour éviter le
# téléchargement (~300 Mo). Sur macOS :
# CHROMIUM_PATH=/Applications/Google Chrome.app/Contents/MacOS/Google Chrome
CHROMIUM_PATH=
BROWSER_TIMEOUT_MS=25000

# La fenêtre du navigateur est visible par défaut : c'est ce qui permet de
# résoudre une vérification Google à la main, et un navigateur masqué est de
# loin le signal le plus facile à détecter. Ne masque la fenêtre que si les
# recherches passent sans être challengées.
# BROWSER_HEADLESS=true

# Temps laissé pour résoudre une vérification Google (défaut 180000, soit 3 min)
CAPTCHA_WAIT_MS=180000

# Serveur
PORT=3000
REFRESH_COOLDOWN_MS=30000
